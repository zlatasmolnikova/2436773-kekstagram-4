const isShorter = (string, size) => string.length <= size;

isShorter('Hello', 5)

const isPalindrom = (sentence) => {
  const original = sentence;
  sentense.replaceAll(' ', '').toUpperCase();
return sentence == original;
}

isPalindrom('топор');
